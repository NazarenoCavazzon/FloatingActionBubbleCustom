name: floating_action_bubble_custom
description: An animated menu using a floating action button, customised to fit the needs of the project.
version: 1.0.0+1
homepage: https://github.com/NazarenoCavazzon/FloatingActionBubbleCustom

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true